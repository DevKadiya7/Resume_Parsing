# Copy to .env and adjust if the backend isn't on the default local port.
# Must include the /api/v1 prefix — the service layer appends resource
# paths (e.g. "/resumes") directly onto this.
VITE_API_BASE_URL=http://localhost:8000/api/v1
