# Resume_Parsing
Backend Resume Parsing Service that accepts PDF resumes, extracts structured candidate information using PyMuPDF, regex, and spaCy, stores normalized data in PostgreSQL, and exposes secure REST APIs for upload, parsing, search, and retrieval. Built with FastAPI, SQLAlchemy, Alembic, Docker, and Pytest following Clean Architecture principles.
